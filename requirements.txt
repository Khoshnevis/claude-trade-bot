# Core runtime dependencies
# MetaTrader5 ships wheels ONLY for Windows (win_amd64). The marker lets deps
# install on Linux/WSL for development; live trading still requires Windows.
MetaTrader5>=5.0.45 ; sys_platform == "win32"   # Official MetaQuotes IPC bridge
numpy>=1.24
pandas>=2.0
PyYAML>=6.0

# Optional but recommended (the bot degrades gracefully if these are missing)
statsmodels>=0.14        # Engle-Granger cointegration / ADF tests for pair selection
hmmlearn>=0.3            # Gaussian HMM regime filter (optional gate)
scikit-learn>=1.3        # meta-label classifier for the gold ML strategy
joblib>=1.3             # persist/load the trained meta-label model
